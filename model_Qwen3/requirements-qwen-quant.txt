# Qwen quantization dependencies
# Install these in an environment with a PyTorch version matching your CUDA/CPU setup.
# For GPU users, install the correct torch wheel from https://pytorch.org first if needed.

transformers>=4.51.0
accelerate>=0.33.0
bitsandbytes>=0.43.3
safetensors>=0.4.5
sentencepiece>=0.2.0
protobuf>=4.25.0
